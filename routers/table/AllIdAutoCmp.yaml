# autocomplete stockids

Edges:
    []

Nodes:
    [
        # 0
        'Loader.parse_task(
            "./routers/tasks/AllIdAutoCmp.yaml",
            kwargs = {
                "opt": "twse",
                "targets": ["stock", "trader"]
            }
        )',

        # 1
        'Loader.parse_task(
            "./routers/tasks/AllIdAutoCmp.yaml",
            kwargs = {
                "opt": "otc",
                "targets": ["stock", "trader"]
            }
        )'
    ]

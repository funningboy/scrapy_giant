# static schedule table

Edges:
    [
        [0,1,1],
        [1,2,1],
        [2,3,1]
    ]

Nodes:
    [
        # 0
        'parse_task(
            "./routers/tasks/HisStock.yaml", 
            kwargs = {
                "opt": "twse",
                "stockids": ["2317"],
            })',
        # 1
        'parse_task(
            "./routers/tasks/HisCredit.yaml",
            kwargs = {
                "opt": "twse",
            })',
    ]




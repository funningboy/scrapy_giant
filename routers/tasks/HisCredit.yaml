{
    # handler/tasks.py hiscredit rand
    task: "collect_hisitem",
    kwargs: {
        opt: null,
        targets: ['credit'],
        starttime: "datetime.utcnow() - timedelta(days=10)",
        endtime: "datetime.utcnow()",
        stockids: [],
        base: 'stock',
        order: "random.sample([''.join(i) for i in list(itertools.product(['+','-'],['bearishused','financeused']))], random.randint(1,4))",
        callback: null,
        limit: "random.randint(100, 200)",
        debug: False
    },
    description: 'find best buy stockids based on credit collected constraint during this time period set'
}
